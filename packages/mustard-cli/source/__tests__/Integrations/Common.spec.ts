import { describe, it, expect, vi, beforeEach } from "vitest";
import { MustardFactory, Context, MustardUtils } from "../../Exports";
import {
  Command,
  RootCommand,
  Option,
  VariadicOption,
  App,
  Ctx,
  Input,
  Inject,
  Utils,
  Options,
} from "../../Exports/Decorators";
import { Validator } from "../../Exports/Validator";
import { CommandStruct, MustardApp } from "../../Exports/ComanndLine";
import { RootCommandHandle } from "../Fixtures/UsageFixtures";

describe("IntegrationTesting:CommonInputs", () => {
  it("should handle common usage", () => {});

  it("should handle command alias ", () => {});

  it("should handle option alias ", () => {});

  it("should handle init value", () => {});

  it("should handle variadic options", () => {});
});
